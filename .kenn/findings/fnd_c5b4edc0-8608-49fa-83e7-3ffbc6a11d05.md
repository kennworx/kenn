---
id: fnd_c5b4edc0-8608-49fa-83e7-3ffbc6a11d05
tags:
- directive
- polarity:do
parent_ids: []
created_at: 2026-07-17T11:19:21.449893Z
---
File-level module docs (the `file_docs` table) are stored as CLEAN PROSE — comment markers stripped at ingestion — so the atlas package `description`, doc search (doc_fts), and embeddings never carry `//!`/`///`/`//` (Rust) or raw doc-comment syntax. Rust: strip_doc_markers in transform_jsonl/records.rs::file_doc_record, applied AFTER the per-block license filter. C# does the equivalent in the dotnet sidecar (clean-doc-prose / NormalizeDocXml). Do NOT reintroduce comment markers into file_doc_record; keep the two languages at parity.