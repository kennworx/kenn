---
id: fnd_9e50c0bf-69e7-4f9a-9b90-4634079d534f
tags:
- guide
parent_ids:
- fnd_1bef0f98-ebc7-4728-bfab-eb2388f1f9c5
- fnd_2dde252e-0ad8-421d-a0a4-1434709258d5
created_at: 2026-06-18T16:42:47.522375Z
---
Dead-CSS report (Phase 3 §9): the `check_css` MCP tool (`kenn-mcp/src/tools/css.rs`) → `kenn-store .../reader/css_health.rs::scan_css_health`. Two pure graph queries over the stylesheet corpus, mirroring `check_links` (bounded: default 100 / max 1000 rows, classes first, full per-category `total` for truncation). `orphan_class` = a `css_class` node with no inbound `uses_css_class` AND no inbound `extends_rule`. `orphan_stylesheet` = a `module` node (language css/sass) with no inbound `imports` AND none of its `defined_in` selectors used (via uses_css_class/extends_rule). KEY gating (task 9.3b) is store-derivable — the reader needs NO config: orphan-class is suppressed unless ≥1 `uses_css_class` edge exists anywhere (the `usage_mining_on` signal: SELECT count(*) FROM edges WHERE kind=uses) — otherwise every class looks orphaned; the tool returns a `note` telling the user to set `[language.css] usage_sources`. CAVEAT: `orphan_stylesheet` with usage mining OFF collapses to 'no inbound imports', which also flags legitimate entry stylesheets (linked from HTML, so they have no inbound graph edge) — only fully accurate once usage mining is on. SQL builds predicate fragments from trusted small int edge-kind codes + literal kind/language strings (no injection); symbols.kind is the db_name string, edges.kind is the numeric code. DEFERRED: 9.3 dangling-code-class (needs a `usage_utilities` allowlist + persisting the usage scan's undefined-token set as dangling stubs, currently counted-and-discarded); 7.3 CSS-Modules `import styles from './x.module.css'; styles.foo` binding.