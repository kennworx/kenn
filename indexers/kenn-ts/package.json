{
  "name": "kenn-ts",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "kenn TypeScript streaming indexer — emits the kenn JSONL wire (indexers/frames.ts).",
  "bin": { "kenn-ts": "src/main.ts" },
  "scripts": {
    "start": "bun run src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typescript": "5.6.2"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
