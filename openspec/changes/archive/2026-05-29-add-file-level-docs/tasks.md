## 1. Producer: emit file-level trivia (C#)

- [x] 1.1 Add `string[]? Doc` to `FileFrame` (Frames.cs) with JSON `doc`; omit when null/empty in `WriteFields`
- [x] 1.2 Add a static `FileDoc.Extract(SyntaxTree)` helper: emit **one entry per contiguous comment block** (consecutive `//` lines coalesced; a blank line breaks the block; each `/* */` is its own block) from `root.GetFirstToken().LeadingTrivia` (file header) + each `BaseNamespaceDeclarationSyntax.GetLeadingTrivia()`, in source order; capture `SingleLineCommentTrivia` and `MultiLineCommentTrivia`; **dedup by trivia `Span`** so a comment above a namespace with no preceding usings is not counted twice
- [x] 1.3 Thread the owning `SyntaxTree` into `FileTracker.RegisterIfNew` (signature `RegisterIfNew(string? path, SyntaxTree? tree)`); extract the doc array once on first sight; pass it into the emitted `FileFrame`
- [x] 1.4 Update the three call sites (IndexerCore.cs:372, :411, :599) to pass the `SyntaxTree` they already hold
- [x] 1.5 Unit-test `FileDoc.Extract` (`indexers/kenn-dotnet.tests/FileDocTests.cs`): contiguous `//` coalesce to one block, `/* */` is one entry with newlines kept, blank line splits header from purpose, ns-comment-only, header+ns, comment above a no-usings namespace deduped, none → null. 7 tests, all green

## 2. Consumer: license filter + file_docs record (Rust)

- [x] 2.1 Add `doc: Vec<String>` (`#[serde(default)]`) to the Rust `FileFrame` (parse_jsonl.rs)
- [x] 2.2 Add a `FileDocsRecord { file_id: ShortId, doc: String }` to kenn-model and wire it through the sink/writer batch buffers (mirror `SymbolDocsRecord`)
- [x] 2.3 Implement the license-boilerplate filter (conservative, per-block) as a small pure function with unit tests for the marker set and false-positive guards (markers expanded to cover multi-paragraph license bodies incl. the warranty clause)
- [x] 2.4 In `transform_jsonl` `Frame::File` arm: filter entries, join survivors with `\n\n`, push a `FileDocsRecord` only when non-empty
- [x] 2.5 Merge dedup — **N/A**: the C# JSONL path emits one `FileFrame` per file via `BatchSink` directly (no `merge::materialize`), so there is no duplicate `file_docs` to dedup

## 3. Store: file_docs dataset (Rust)

- [x] 3.1 Add `GraphDataset::FileDocs` (schema.rs) with `as_str = "file_docs"`, an Arrow schema `{ file_id: u32, doc: Utf8 }`, and a `file_docs_batch` builder
- [x] 3.2 Add a `scan_file_docs` reader + writer append path (mirror `scan_symbol_docs` / symbol_docs write)
- [x] 3.3 No `STORE_SCHEMA_VERSION` bump (no users) — `file_docs` is added in place; a stale snapshot is regenerated by reindex

## 4. Search: path-identified doc rows (Rust)

- [x] 4.1 Extend `build_knowledge_store` (writer.rs) to scan `file_docs`, join `file_docs.file_id → files.id` for path + language, and emit a `Doc` row per file doc
- [x] 4.2 Emit file-doc rows via a dedicated `build_file_doc_rows` (schema.rs) — cleaner than overloading `build_batch_rows`: `embed_key = "filedoc:<lang>:<path>"`, `pub_id = ""`, `path = <workspace-relative path>`, `id = <file's real id>`, `doc_text`, fingerprint; populates the previously-null `path` column
- [x] 4.3 Thread the file-row `language` (from the joined files row) into the emitted row

## 5. MCP: file hits in search results (Rust)

- [x] 5.1 `BlendedHit` enum (`Symbol`/`File`) in kenn-store + `BlendedFileRow`; wire `SearchHitRef` (untagged, `result_kind:"file"`) + `RankedFileRef` in kenn-mcp. New reader method `search_blended_hits` (file-inclusive); `search_symbols_blended` stays symbol-only so existing callers/tests are unaffected
- [x] 5.2 Partition hits by kind before hydrating (db/reader.rs): a `SearchHit` with empty `pub_id` is flagged `is_file` and hydrates from the `files` dataset via `fetch_file_row(id) -> FileRow`, NEVER `id → SYMBOLS` (D6). `merge_hits` keyed by `(is_file, id)` so the id spaces never collide
- [x] 5.3 `search_symbols` AND `semantic_search` interleave file + symbol hits with the `result_kind` discriminant (`semantic_search.code` is now `Vec<SearchHitRef>`); `find_symbol` and `find_similar` stay symbol-only
- [x] 5.4 MCP integration test (`search_symbols_returns_file_hit_for_header_token`): corpus with a `file_docs` row, asserts `search_symbols` returns a `SearchHitRef::File` tagged `result_kind:"file"` with the right path
- [x] 5.5 Regression test for D6: a file and a symbol sharing the same `id` number — a file-doc hit resolves to the file, not the symbol (`file_doc_hit_resolves_to_file_not_same_numbered_symbol`)

## 6. End-to-end validation

- [x] 6.1 `cargo clippy --workspace --all-targets` clean (pedantic)
- [x] 6.2 `just crap-ci` green (fixed the `find_similar_symbols` regression — D6 guard bumped cyclomatic; loop rewritten as an iterator chain to net it out). Re-run after the semantic_search change pending
- [x] 6.3 A standalone file-doc row has no paired name row; its fingerprint is per-row (`build_file_doc_rows`), and reconcile is a no-op without a sidecar — exercised by the D6 test through `build_knowledge_store`
- [x] 6.4 Validated via the C# fixture (`tmp/cs-fixture`): the file header flows producer → filter → search row. NOTE: a live reindex of *this* workspace can't exercise it — file_docs is C#-only and this repo is Rust (Rust `//!` flows through `symbol_docs`, design D8)
- [x] 6.5 `cargo fmt --all` as the final step
