//! SQL indexing: the shared text-to-references extractor, table identity,
//! and the `.sql` producer that consumes both.

pub mod emit;
pub mod ingest;
pub mod mint;
pub mod parse;
pub mod registry;
